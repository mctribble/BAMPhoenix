//beforeEach(module('bam'));
     //describe('associateViewControllerTest', function() {
     //    var scope;
     //    var SessionServiceMock;
     //    var Controller;
     //
     //    beforeEach(function() {
     //        SessionServiceMock = {
     //            name : 'theara'
     //        };
     //    });
     //    beforeEach(inject(function($rootScope, $controller) {
     //        scope = $rootScope.$new();
     //        Controller = $controller('associateViewController', {
     //            $scope : scope,
     //            DataService : SessionServiceMock
     //        });
     //    }))
     //
     //    describe('Retrieving associates for a particular batch given a batch id.', function() {
     //        it('response status 200', inject(function($httpBackend) {
     //            spyOn(SessionServiceMock, 'something').and.callThrough();
     //        }));
     //    });
     //});